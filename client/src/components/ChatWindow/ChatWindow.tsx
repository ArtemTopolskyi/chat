import React, { FC } from 'react';
import { Chat } from '../../typedefs';

interface Props {
  chat: Chat | null;
}

export const ChatWindow: FC<Props> = ({ chat }) => {


  return (
    <></>
  );
};
